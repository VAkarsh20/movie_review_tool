def get_post_description(proto, show_year=False):

    res = proto.title
    
    if show_year:
        res += " {}".format(proto.release_year)

    "#foryoupage #fyp #moviereview #rating #film #filmtok #movie #movietok #capcut"

    return res